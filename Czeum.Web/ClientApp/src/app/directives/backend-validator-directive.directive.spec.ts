import { BackendValidatorDirectiveDirective } from './backend-validator-directive.directive';

describe('BackendValidatorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new BackendValidatorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
