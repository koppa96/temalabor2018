import { ReverseMatchValueDirective } from './reverse-match-value.directive';

describe('ReverseMatchValueDirective', () => {
  it('should create an instance', () => {
    const directive = new ReverseMatchValueDirective();
    expect(directive).toBeTruthy();
  });
});
